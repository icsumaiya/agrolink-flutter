import 'package:flutter/material.dart';

/// Brand + semantic colors. Widgets must read UI colors from
/// Theme.of(context).colorScheme; only use these for brand/status colors.
class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF2E7D32); // leaf green
  static const Color secondary = Color(0xFFF9A825); // harvest amber
  static const Color onSecondary = Color(0xFF1B1B1B);

  // Order status colors (used by StatusBadge later)
  static const Color pending = Color(0xFFF9A825);
  static const Color accepted = Color(0xFF1E88E5);
  static const Color preparing = Color(0xFF8E24AA);
  static const Color completed = Color(0xFF2E7D32);
  static const Color rejected = Color(0xFFD32F2F);
  static const Color cancelled = Color(0xFF757575);
}